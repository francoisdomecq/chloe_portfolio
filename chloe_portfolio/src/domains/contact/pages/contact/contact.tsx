import { NavTabs } from "../../../core/types";

import "./contact.scss";
const Contact = () => {
    return (
        <section className="contact" id={NavTabs.CONTACT}>
          contact
        </section>
    );
};

export default Contact;

