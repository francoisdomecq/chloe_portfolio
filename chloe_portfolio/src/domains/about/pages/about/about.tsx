import { NavTabs } from "../../../core/types";

import "./about.scss";
const About = () => {
    return (
        <section className="about" id={NavTabs.ABOUT}>
          about
        </section>
    );
};

export default About;

